// TODO: replace stubs with Supabase auth when integrating
export async function signIn(_email: string, _password: string) {
  // TODO: supabase.auth.signInWithPassword({ email, password })
}

export async function signUp(_email: string, _password: string, _username: string) {
  // TODO: supabase.auth.signUp({ email, password, options: { data: { username } } })
}

export async function signOut() {
  // TODO: supabase.auth.signOut()
}
